apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeguard-proxyserver.fullname" . }}
  labels:
    {{- include "kubeguard-proxyserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubeguard-proxyserver.selectorLabels" . | nindent 4 }}
